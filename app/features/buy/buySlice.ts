import { createSlice } from '@reduxjs/toolkit';
/* eslint-disable */

const buySlice = createSlice({
  name: 'buy',
  initialState: {
    numOfItems: 0,
    items: []
  },
  reducers: {},
});

export default buySlice.reducer;
